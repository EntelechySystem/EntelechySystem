# 开发日志（EntelechySystem）

> 文档定位：记录“做了什么、为什么做、影响范围、如何验证”，便于回溯与协作。

## 日志规范

每条日志建议包含：

- **日期**：YYYY-MM-DD
- **类型**：特性 / 修复 / 重构 / 文档 / 依赖 / 配置 / 实验
- **范围**：模块/目录（例如 `EntelechySystem/engine`）
- **内容**：做了什么 & 为什么
- **影响**：是否破坏兼容、是否影响实验结果/输出结构
- **验证**：运行了哪些脚本/做了哪些检查

## 2026-08-18

- 类型：依赖 / 迁移
- 范围：`pyproject.toml`、`scripts/uv_migrate.sh`、根目录与 `docs/`
- 内容：按 `scene-kit` 上游通知完成包名迁移：发行依赖由 `world-model-kit` 改为 `scene-kit`，本地可编辑路径由 `../world-model-kit` 改为 `../scene-kit`，导入验证由 `world_model_kit` 改为 `scene_kit`。历史日志中的旧名称保留用于追溯。
- 影响：依赖解析与安装改用已存在的 `../scene-kit` 仓库；`scene-kit` 公共 API 签名不变。
- 验证：确认 `../scene-kit` 目录存在，并检查仓库内旧依赖配置已无残留。

## 2026-07-31

- 类型：配置 / 文档
- 范围：`pyproject.toml`、`README.md`、`docs/开发者指南.md`
- 内容：Python 工具包（distribution）名称由 `entelechysystem` 统一改为 **`entelechy`**（见 `pyproject.toml` 的 `name` 字段）。源码包目录与导入路径（`EntelechySystem/`）不变。
- 影响：仅影响包元数据命名，不影响源码导入与运行。
- 验证：`uv tree` / `uv run python -c "import entelechysystem"` 不再适用，需以 `entelechy` 为准；源码导入仍为 `EntelechySystem.*`。

- 类型：依赖 / 重构
- 范围：`pyproject.toml`、`scripts/`、根目录与 `docs/`
- 内容：将环境迁移至 uv (Python 3.14) 并彻底整理依赖体系。
  - `pyproject.toml`：直接依赖从 15 个精简为 6 个，依据源码真实 import 推断（`chardet`/`numpy`/`openpyxl`/`pandas`）；新增 `[tool.uv.sources]` 以 editable 方式链接本地依赖 RECS（`../relation-entity-component-system`）与 `world-model-kit`（`../world-model-kit`）；`torch` 移入可选依赖（模型无 torch 时自动退化为 NumPy 实现）。
  - 移除孤立依赖：`gymnasium`、`jupyter`、`jupyterlab`、`matplotlib`、`pandas-stubs`、`pettingzoo`、`pip`、`pygame`、`scipy`、`torchaudio`、`torchvision`（源码中未出现 import）。
  - 新增 `scripts/uv_migrate.sh`：一键迁移脚本（备份 → 清理 → 重建 → 验证，幂等，支持 `--mirror` 清华镜像）。
  - `.gitignore`：新增 `old_requirements.txt`。
- 影响：安装方式由 `requirements.txt`（已废弃）改为 `pyproject.toml` + `uv.lock` 单一来源；文档路径由 `EntelechySystem_python/` 统一为 `EntelechySystem/`。不破坏代码运行逻辑。
- 验证：`bash scripts/uv_migrate.sh`（备份、清理、重建、导入验证、运行示例实验）。

## 2026-02-18

- 类型：文档
- 范围：`docs/`
- 内容：新增开发者指南、用户手册、开发日志、需求文档，用于补齐 README 中“使用方法 TODO”的缺口。
- 影响：不影响代码运行，仅新增文档。
- 验证：检查文档引用的入口脚本与配置目录在仓库中存在。

## 历史版本信息（摘录）

- `v0.0.8_alpha`：版本号来源于根目录 `pyproject.toml` 与 `README.md`。

## 模板（复制使用）

### YYYY-MM-DD

- 类型：
- 范围：
- 内容：
- 影响：
- 验证：
